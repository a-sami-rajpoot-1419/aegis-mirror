{
  "name": "mirror-vault-contracts",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run --network mirrorVaultLocal scripts/deploy.ts",
    "fund:local": "hardhat run --network mirrorVaultLocal scripts/fundWallets.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "bech32": "^2.0.0",
    "hardhat": "^2.22.18",
    "typescript": "^5.7.3"
  }
}
